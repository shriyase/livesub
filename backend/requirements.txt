fastapi==0.115.0
uvicorn[standard]==0.30.6
python-dotenv==1.0.1
openai==1.54.0
numpy==1.26.4
websockets==13.1
httpx==0.27.2

# Optional local ASR fallback - uncomment to use faster-whisper
# faster-whisper==1.0.3

# Required only if SINHALA_ASR_MODEL is set (routes Sinhala to a fine-tuned
# Whisper checkpoint loaded locally via the transformers pipeline).
# Heavy install (~2GB with torch). Comment out if you don't need Sinhala.
transformers==4.46.2
torch==2.5.1
